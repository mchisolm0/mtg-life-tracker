# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

# Native development builds

This app uses native dependencies such as react-native-mmkv, so do not use Expo Go for runtime validation. Use an Expo development build with expo-dev-client.

# Visual proof

For UI or runtime behavior changes, capture a screenshot or short screen recording from the simulator/device and include the artifact path or PR attachment in the handoff. Prefer video when validating gestures, persistence after restart, or offline/reconnect behavior.
